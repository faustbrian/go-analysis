schema_version: 1
tool_version: v1.0.13
tool_checksums_sha256: 164e5c8abefe655958f522677856816faddbd2a8d40eba7d37ba5a031210df70

manifest:
  modules: modules.json
  packages: packages.json

evidence:
  root: ".verification"

mutation:
  root: ".verification/mutation"
  imports:
    - module: "."
      archive: ".verification/mutation/bootstrap/root.zip"
      ledger: ".verification/mutation/migration-ledger.json"

operations:
  - module: "."
    gate: api
    steps:
      - type: make
        makefile: verification/package.mk
        target: compatibility
        timeout: 20m
  - module: "."
    gate: test
    steps:
      - type: make
        makefile: verification/package.mk
        target: package-specific
        timeout: 20m
  - module: "."
    gate: docs
    steps:
      - type: make
        makefile: verification/package.mk
        target: docs
        timeout: 20m
  - module: "."
    gate: fuzz
    steps:
      - type: go-test
        packages:
          - ./analysis
        fuzz: '^FuzzDecodeConfig$'
        budget: 10000x
        timeout: 20m
      - type: go-test
        packages:
          - ./analysis
        fuzz: '^FuzzParseSuppressions$'
        budget: 10000x
        timeout: 20m
      - type: go-test
        packages:
          - ./analysis
        fuzz: '^FuzzReportWriters$'
        budget: 10000x
        timeout: 20m
      - type: go-test
        packages:
          - ./analysistestkit
        fuzz: '^FuzzAnalyzersNoPanic$'
        budget: 10000x
        timeout: 20m
  - module: "."
    gate: benchmark
    steps:
      - type: go-test
        packages:
          - ./analysis
          - ./analysistestkit
        benchmark: .
        budget: 1s
        timeout: 20m
